{% extends "base.html.twig" %}


{% block body %}
<h1>Les planètes</h1>

<div>
    {% for planete in planetes %}
        <div>
            <h2>{{ planete.nom }}</h2>
            <p>{{ planete.galaxie.nom??null }}</p>
            <p>Taille: {{ planete.taille }} km</p>
            <p>Distance au soleil :
            {{ planete.distance }} UA</p>
            <a href="{{ path('planete_show', {id: planete.id}) }}">Voir</a>
        </div>
    {% endfor %}
</div>
{% endblock body %}
