{% extends 'base.html.twig' %}

{% block title %}Planete{% endblock %}

{% block body %}
    <h1>Planete</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ planete.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ planete.nom }}</td>
            </tr>
            <tr>
                <th>Taille</th>
                <td>{{ planete.taille }}</td>
            </tr>
            <tr>
                <th>Distance</th>
                <td>{{ planete.distance }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ planete.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_planete_bis_index') }}">back to list</a>

    <a href="{{ path('app_planete_bis_edit', {'id': planete.id}) }}">edit</a>

    {{ include('planete_bis/_delete_form.html.twig') }}
{% endblock %}
