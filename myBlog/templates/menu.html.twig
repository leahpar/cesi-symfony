<nav>
  <a href="{{ path('helloworld') }}">Hello world</a>
  <a href="{{ path('post_list') }}">Liste des posts</a>
  <a href="{{ path('post_random') }}">Un post au hasard</a>
  <a href="{{ path('post_last') }}">Dernier post consulté</a>

  {% if is_granted('ROLE_AUTEUR') %}
  <a href="{{ path('post_list_me') }}">Liste de mes posts</a>
  <a href="{{ path('post_new') }}">Créer un post</a>
  {% endif %}

  {% if is_granted('ROLE_ADMIN') %}
    <a href="{{ path('admin_index') }}">Admin</a>
    Auteurs :
    <a href="{{ path('author_index') }}">Lister</a>
    <a href="{{ path('author_new') }}">Créer</a>
  {% endif %}

  {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
    <a href="{{ path('app_logout') }}">Déconnexion</a>
  {% else %}
    <a href="{{ path('app_login') }}">Connexion</a>
    <a href="{{ path('app_register') }}">Inscription</a>
  {% endif %}


</nav>
